name: memory_app
description: A new Flutter project.

dependencies:
  flutter:
    sdk: flutter
  flutter_flux:
    path: ../flutter_flux

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
